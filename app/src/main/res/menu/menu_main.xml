<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context="com.cse4322.mockstock.MainActivity" >
    <item
        android:id="@+id/app_bar_search"
        android:title="@string/search_bar"
        app:actionViewClass="android.support.v7.widget.SearchView"
        android:icon="@drawable/ic_search_black_24dp"
        app:showAsAction="always"
        tools:ignore="AppCompatResource"
        android:visible="true"
        android:enabled="true"
        android:actionLayout="@color/cardview_light_background" />

    <item android:id="@+id/reset_account"
        android:title="@string/menu_reset_account"
        android:orderInCategory="101"
        android:actionLayout="@layout/activity_portfolio"
        app:showAsAction="never|withText"
        android:checked="false" />

</menu>

